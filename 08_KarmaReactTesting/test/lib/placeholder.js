/*
 * @author David Menger
 */
'use strict';

const assert = require('assert');

describe('example test', function () {
    it('should work', function () {
        assert.ok(true, 'it should work');
    });
});
